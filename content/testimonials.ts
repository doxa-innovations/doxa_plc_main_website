import type { Testimonial } from "./types";

/**
 * Client testimonials. EMPTY until real, attributable quotes are collected
 * from clients (ZOA, KLA, MLTS, etc.), see the pre-launch checklist. Sections
 * that render testimonials should hide themselves when this array is empty
 * rather than show placeholder quotes.
 */
export const TESTIMONIALS: Testimonial[] = [];
